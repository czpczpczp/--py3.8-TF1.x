import tensorflow as tf
import numpy as np

'''y = np.array([[1, 2, 3], [4, 5, 6]])
t = np.array([1, 0, 1])

a = tf.reduce_mean(tf.compat.v1.to_float(np.equal(np.argmax(y, axis=0), t)))
print(a)
print(np.argmax(y, axis=0))'''





